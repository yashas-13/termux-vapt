# Databricks PoC Reproduction Steps

## Prerequisites
- Workspace: `https://dbc-20c3ac0f-c5f4.cloud.databricks.com` (org `7474647923123844`)
- PAT: `<REDACTED-PAT>` (generated by `cyberjeet13@gmail.com` — admin)
- Secondary target: `https://dbc-9a3f8ed1-7608.cloud.databricks.com` (org `8789365059946363`, account `cf91c43a-0b45-4d67-aaf8-45dd79970a1d`)

---

## Finding 1: Unity Catalog Storage Root Disclosure (Medium / CVSS 4.3)

**Steps:**
```bash
# 1. List catalogs
curl -s -H "Authorization: Bearer <REDACTED-PAT>" \
  https://dbc-20c3ac0f-c5f4.cloud.databricks.com/api/2.1/unity-catalog/catalogs | jq .

# 2. Observe storage_root + metastore_id in response
# 3. Storage credentials
curl -s -H "Authorization: Bearer <REDACTED-PAT>" \
  https://dbc-20c3ac0f-c5f4.cloud.databricks.com/api/2.1/unity-catalog/storage-credentials | jq .

# 4. External locations
curl -s -H "Authorization: Bearer <REDACTED-PAT>" \
  https://dbc-20c3ac0f-c5f4.cloud.databricks.com/api/2.1/unity-catalog/external-locations | jq .
```

**Expected screenshot:** Terminal showing JSON with `storage_root: "s3://dbstorage-prod-0yrhy/uc/485c2e6d-8ca8-4cc0-8908-f30c4dff7620/d6f0705d-6a90-4ee0-9581-53198b52313d"` highlighted.

---

## Finding 2: CORS Wildcard on All API Endpoints (Info → High if chained)

**Steps:**
```bash
# Target 1 (primary)
curl -i -H "Origin: https://evil.com" \
  -H "Authorization: Bearer <REDACTED-PAT>" \
  https://dbc-20c3ac0f-c5f4.cloud.databricks.com/api/2.1/unity-catalog/catalogs

# Target 2 (secondary)
curl -i -H "Origin: https://evil.com" \
  -H "Authorization: Bearer invalid" \
  https://dbc-9a3f8ed1-7608.cloud.databricks.com/api/2.0/clusters/list

# Preflight check (both)
curl -i -X OPTIONS -H "Origin: https://evil.com" \
  -H "Access-Control-Request-Method: POST" \
  -H "Access-Control-Request-Headers: authorization,content-type" \
  https://dbc-20c3ac0f-c5f4.cloud.databricks.com/api/2.0/clusters/list
```

**Expected screenshot:** Response headers showing `access-control-allow-origin: *` and `access-control-allow-headers: Authorization, ...`

---

## Finding 3: Weak CSP on Login Page (Info)

**Steps:**
```bash
curl -i https://dbc-20c3ac0f-c5f4.cloud.databricks.com/login.html?o=7474647923123844 | grep -i content-security
```

**Expected:** `default-src *; script-src 'self' 'unsafe-eval' ...; frame-ancestors *`

---

## Finding 4: Workspace Token Isolation Works (Negative — proves scope boundary)

**Steps:**
```bash
# Correct host → 200
curl -s -o /dev/null -w "%{http_code}" \
  -H "Authorization: Bearer <REDACTED-PAT>" \
  https://dbc-20c3ac0f-c5f4.cloud.databricks.com/api/2.1/unity-catalog/catalogs

# Wrong host → 403
curl -s -o /dev/null -w "%{http_code}" \
  -H "Authorization: Bearer <REDACTED-PAT>" \
  https://dbc-9a3f8ed1-7608.cloud.databricks.com/api/2.1/unity-catalog/catalogs
```

---

## Negative Results — Tested and Not Critical (document for reviewer)

| Test | Command | Result | Why Not $10k |
|---|---|---|---|
| Org-Id spoof | `curl -H "X-Databricks-Org-Id: 8789365059946363" .../catalogs` | Same 3 catalogs | Header ignored |
| OIDC SSRF | `client_id=http://169.254.169.254` | `invalid_client` | No metadata SSRF |
| Driver-proxy IDOR | `driver-proxy/o/747.../0/0` brute | All 303 → login | No access |
| Cluster RCE | `POST /clusters/create` with init_script | 400 no worker env | Free Edition by-design |
| Lakebase escape | `/postgres/*`, `/lakebase/*`, `/oltp/*` | 404 | Not provisioned |
| Secret extraction | `secrets/list?scope=pentest-scope-1234` | `{}` empty | No secrets stored |

---

## Screenshots Included

| File | Description |
|---|---|
| `screenshot-cover.png` | Full HTML report (1280x900) |
| `01-catalogs.json` | Unity Catalog response with storage_root |
| `02-storage-cred.json` | Managed storage credential |
| `03-external-loc.json` | External S3 location |
| `04-scim-me.json` | SCIM user = admin |
| `05-cors-headers.txt` | CORS wildcard on catalogs API |
| `06-csp-headers.txt` | Login page CSP headers |
| `06-login-csp.txt` | Full login page + CSP |
| `07-w1-cors.txt` | CORS on secondary target |

---

## How to Attach to HackerOne

1. Create report → select **Databricks Product** asset
2. Severity: **Medium** (storage_root disclosure) or **Info** (CORS/CSP)
3. Attach all files from `/bhdb2/poc/`
4. In description, paste the curl blocks verbatim
5. Reference: `metastore_id: d6f0705d-6a90-4ee0-9581-53198b52313d` (from user's `aws:us-east-2:d6f0705d-...` OpenShare)

---

## Do Not Submit As Critical ($10k)

- No unauth RCE reproduced
- No cross-tenant data leak (header spoof returns same data)
- No control plane secret extracted
- No Lakebase tenant escape (not provisioned)

Submitting as Critical without valid PoC = N/A per program rules. Submit honestly as Medium/Info.