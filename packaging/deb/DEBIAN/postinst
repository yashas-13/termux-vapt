#!/system/bin/sh
set -e
if [ -z "${PREFIX:-}" ]; then
  PREFIX=/data/data/com.termux/files/usr
fi
# No-op: ensure binary is executable and record install
echo "termux-vapt installed."
echo "Add auth gate before first scan: termux-vapt auth --help"
